# 项目开发约定

- 每次修改完代码，都要执行 `git commit`，并用中文描述清楚这次变动做了什么。
- 提交只包含当前任务的文件，不要混入、覆盖或撤销用户及其他任务的改动。
- 功能分支默认使用 `codex/` 前缀。实现与验证完成后更新 README 中的项目状态和开发命令。
- 本仓库负责独立 Shopify 管理 App 与后端。主题和客户配置器在 `umx-module-configurator` 仓库维护，两者通过接口集成。
- 这是公开仓库。不得提交真实密钥、访问令牌、客户数据、内部价格、生产配置、数据库备份或未经明确授权公开的模型资产。配置示例只使用占位值。

## 用户分类与服务端权限

- Shopify 客户标签按小写完整名称匹配，优先级为 `nb` → `internal`、`tob` → `business`、默认 → `consumer`。
- 客户端传入的分类、用户编号、隐藏页面、独立路由或动态加载都不是授权依据。
- 管理接口和客户接口各自校验可信身份与操作权限。只向客户返回其有权查看的数据，不能把全部渠道价格下发后再隐藏。
- 客户专属接口不得使用共享缓存。不要将内部数据写入公开前端资源或日志。
- 显示价格的变更不等于 Shopify 结账价格已生效，必须分别验证。

## 配置器集成与验收

- 公共核心不能依赖企业版或内部版专属模块。专属重功能仅在对应用户实际打开时加载。
- 零售入口不得挂载内部组件、请求内部数据或启动内部运行任务；卸载时清理定时器、订阅和 Worker。
- 涉及前端集成时，检查构建产物与网络请求，确认内部代码和资源没有进入零售初始加载链路。
- 本地身份模拟、真实 Shopify 登录标签、App 安装、部署可达性和真实结账分别记录，不能相互替代验收。
- 针对真实权限边界、价格规则和发布回退等关键逻辑进行有意义的验证；文档初始化无需制造空壳测试。
