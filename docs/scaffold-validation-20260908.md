# 应用框架与开发配置验证（2026-09-08）

## 已完成

- 导入官方 React Router + TypeScript 模板，保留登录、嵌入式页面和 Prisma 会话模型；移除演示商品创建、价格修改及无关示例页面。
- 项目内安装 Shopify CLI 4.7.1，使用 Node.js 22.22.0。
- 已关联用户在 Dev Dashboard 创建的应用，没有重复创建 App。
- 本地会话库迁移成功；再次执行后确认没有待执行迁移。初次迁移曾出现无详细信息的 schema engine 错误，后续重试及正常命令均通过，未据此推断根因。
- `npm run check` 通过：ESLint、TypeScript 和客户端/服务端生产构建。
- 依赖修复后的安装审计为 0 项；生产依赖审计为 0 项。
- 本地首页和 Cloudflare 临时 HTTPS 首页均返回 HTTP 200；浏览器可见 `Configurator Dashboard` 登录入口。
- 未带认证信息访问 `/app` 返回 HTTP 410，未暴露管理页；未签名的卸载 Webhook POST 返回 HTTP 400。此项只验证拒绝未认证请求，真实登录另外记录。
- 在浏览器发布 `configurator-dev-20260908`，Dev Dashboard 显示该版本有效。
- 已配置应用名称、可访问的临时 HTTPS 地址、嵌入模式、`/auth/callback` 和 Webhook API `2026-07`；商品和客户数据权限为空，旧版安装流程未启用。
- `.env`、`.env.development`、本地应用配置与 SQLite 会话库均被 Git 忽略；凭据文件权限限制为当前用户读写。
- 用户自行确认安装后，浏览器实际进入 Shopify 后台的 `Configurator Dashboard` 应用；嵌入页面显示“配置器管理”及初始化说明。已验证安装和首次真实嵌入式登录成功，未将会话参数或访问令牌写入记录。

## 尚未完成

- 会话续期与重新登录验证。
- 生命周期 Webhook 的远端订阅与签名回调验证。当前只有处理入口及公开配置示例；本次浏览器发布并未添加这些订阅。
- 模型上传、分类、用户权限、渠道价格与结账的业务实现。
- Docker 镜像构建/运行验收、PVE 部署、生产持久化与正式域名。

当前开发入口依赖本机服务和临时隧道存活，不能作为正式生产可用性结论。
